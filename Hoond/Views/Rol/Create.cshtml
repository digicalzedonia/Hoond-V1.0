@model Hoond.Models.Rol
@using Hoond.app_GlobalResources;
@{
    ViewBag.Title = "Create";
}
<div class="icon-create"><h2>@Content.titulo_CreaRol</h2></div>
@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <div class="grid">
        <div class="grid_row">
        <div class="grid_item">
            <div class="editor-label">
                @Html.LabelFor(model => model.descRol)
            </div>
            <div class="editor-field">
                @Html.EditorFor(model => model.descRol)
                @Html.ValidationMessageFor(model => model.descRol)
            </div>
        </div>
        </div>
        <div class="grid_row">
            <div class="grid_item center">
                <input type="submit" value="@Content.submit_Crear" />
            </div>
            <div class="grid_item center">
                <span class="link-txt back">@Html.ActionLink(Content.link_Regresar, "Index")</span>
            </div>
        </div>
    </diV>
}


